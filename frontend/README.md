# frontend/ — React + TypeScript

Not created yet — placeholder. Phase 1 only needs: upload a PDF, ask a
question, see the answer with page-number sources. Don't build the
multi-course switcher, job status UI, or source-viewer polish (those are
Phases 2, 5) until their phase comes up.
