# backend/ — Java Spring Boot

Owns: auth, course/document CRUD, file upload (streams to R2), ingestion
job row creation, chat endpoint (retrieval + LLM call), source lookup.

Not created yet — this is a placeholder. First real task for Claude Code /
Codex on this repo: propose the Spring Boot module layout here (Phase 1,
per AGENTS.md), scoped to what Phase 1 needs (text-only PDF RAG, single
course) — don't scaffold multi-course or video/image handling yet.
